{{!< default}}

{{#contentFor 'htmlLang'}}{{@site.locale}}{{/contentFor}}

{{#contentFor 'styles'}}
<link rel="stylesheet" type="text/css" href="{{asset "css/pages.css"}}" />
{{/contentFor}}

{{#contentFor 'title'}}{{meta_title}}{{/contentFor}}

{{#post}}
<div class="mds-page">
    <div class="mds-canvas">
        <h1>{{title}}</h1>
        <div class="mds-content">
            {{content}}
        </div>
    </div>
</div>
{{/post}}
